<Project Sdk="Microsoft.NET.Sdk.Razor">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows10.0.19041.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
    <RootNamespace>QMusic.Desktop</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- Override the default build action so WPF doesn't auto-generate Main() —
         we define our own entry point in Program.cs for DI setup -->
    <ApplicationDefinition Remove="App.xaml" />
    <Page Include="App.xaml" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Components.WebView.Wpf" Version="10.0.50" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\QMusic.Application\QMusic.Application.csproj" />
    <ProjectReference Include="..\QMusic.Infrastructure\QMusic.Infrastructure.csproj" />
  </ItemGroup>

</Project>
