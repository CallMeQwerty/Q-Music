@using QMusic.Desktop.Components.Layout
@using QMusic.Desktop.Components.Search

<!--
    For this v1 shell, we use a simple single-page layout.
    No Router needed yet — we just render the main layout with search as the content.
    When the app grows to need multiple pages, we'll add Blazor routing here.
-->
<MainLayout>
    <Body>
        <SearchView />
    </Body>
</MainLayout>
